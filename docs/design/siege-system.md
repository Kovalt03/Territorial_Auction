# 공성전 시스템 상세 설계 (초안)

> 상태: 초안 — 골격 3개는 확정, 세부 수치·신규 유닛·보호 기간 값은 검토 중(TBD 표시).
> 확정 후 `docs/design/military-specs.md`에 반영.

## 0. 확정 골격 (2026-07-13)

| 항목 | 결정 |
|---|---|
| Zone 진행 | **외곽 → 중심** (Zone 3 → 2 → 1). 안쪽 Zone은 바깥 Zone을 클리어해야 진입 |
| 판정 모델 | **성 HP 누적·다회 공성** — 한 번의 공격으로 결판나지 않고 건물 HP를 깎아 나감 |
| 방어 | **사전 배치 중심** — 방어 건물·유닛 사전 배치 + 성 HP·수리로 대비(카운트다운 중 능동 대응 없음) |

---

## 1. 용어 · Zone 정의

영토 격자는 중심으로부터 체비쇼프 거리로 3개 Zone (`ZonePolicy.calculateZone`):

| Zone | 위치 | 대표 건물 | 공략 목표 |
|---|---|---|---|
| **Zone 3** | 외곽 | 저장소(Storage), 농경지 | 자원 약탈 |
| **Zone 2** | 중간 | 생산소(Workshop), 병영(Barracks) | 생산 마비 |
| **Zone 1** | 중심 | **성(Castle)** | 영토 점령 |

방어 건물(방벽 WALL def30, 타워 TOWER def60)은 어느 Zone에나 배치 가능하며, 배치된 Zone의 방어력에 기여한다.

---

## 2. 공격 흐름

```
① 공격권 소모 + 유닛 지정 → 공성 선언
② 30분 카운트다운 (SIEGE_COUNTDOWN_MINUTES) — 방어자에게 알림
③ 스케줄러 일괄 판정 (resolveAt 도달)
④ 결과 적용 + 양측 알림 → (성 미파괴면) 재공성 반복
```

- **공격권**: 일반(zone 공격) / 정밀(목표 건물 지정). 선언 시 1개 소모.
- **선언 검증**: 자기 영토 아님 · 점유 중 · **보호 기간 아님** · 재공격 쿨다운 아님 · **바깥 Zone 클리어됨** · 공격권 보유 · 대기 유닛 보유.
- 판정은 선언 시점이 아니라 **카운트다운 종료 시점**의 배치 상태로 계산 → 방어자에게 대비 시간을 준다.

---

## 3. Zone 진행 규칙 (외곽 → 중심)

- Zone 3부터 시작. **Zone N은 Zone N+1(더 바깥)이 "클리어"된 뒤에만 공격 가능.**
  - Zone 3: 전제 없음
  - Zone 2: Zone 3 클리어 필요
  - Zone 1: Zone 2 클리어 필요
- **클리어 판정**: 해당 Zone 방어 건물의 `Σ현재HP / Σ최대HP < (1 − ZONE_CLEAR_THRESHOLD)` (기본 0.5 → HP 절반 이하로 붕괴 시 클리어).
- ⚠️ **코드 수정 필요**: 현재 `validateZoneCleared`는 `attackZone-1`(안쪽)을 요구 → **`attackZone+1`(바깥)** 요구로 반전해야 한다.

---

## 4. 전투 판정식

한 번의 공성 판정:

```
ATK = Σ(파견 유닛 attackPower × 수량)
DEF = Σ(방어 유닛 defensePower × 수량) + Σ(해당 Zone 방어 건물 defensePower)   ※건설 중 건물 제외
```

- **성공 조건**: `ATK > DEF`
- **건물 데미지(성공 시)**: 그 Zone 대상 건물 HP를 감소.
  - 총 건물 피해 = **`Σ(생존 공격 유닛 buildingDamage × 수량)`** — 공성 병기가 핵심. (교전 승패는 ATK/DEF, 돌파량은 buildingDamage로 분리)
  - 정밀 공격: 지정 건물에 집중. 일반 공격: Zone 내 건물에 분산.
  - 교전에서 잃은 공격 유닛은 건물 피해에 기여하지 않음 → 호위가 뚫려 공성 병기가 죽으면 돌파 실패.
- **유닛 손실**: 성공 시 공격자 `ATTACKER_LOSS_RATE 30%`·방어자 `DEFENDER_LOSS_RATE 30%`, 실패 시 공격자 `ATTACKER_FAIL_LOSS_RATE 50%`.
- **다회 공성**: 성 HP가 남아 있으면 쿨다운 후 재선언해 계속 깎는다. 방어자는 그 사이 수리·증원 가능.

---

## 5. Zone별 승리 효과

| Zone | 성공 시 즉시 효과 |
|---|---|
| **Zone 3** | 저장소 GP `LOOT_RATE 50%` 약탈 → **공격자 금고** 적립 |
| **Zone 2** | 생산소/병영 HP 감소, 파괴 시 `WORKSHOP_DEBUFF_HOURS 12h` 생산 중단 디버프 |
| **Zone 1** | 성 HP 감소. **성 HP 0 → 공격자 즉시 인계**(아래) |

## 6. 성 파괴 = 공격자 즉시 인계 (Stage 8 구현 완료)

성 HP 0 시 경매 없이 공격자에게 즉시 이전:
- 저장 GP **80%(`TERRITORY_LOSS_TRANSFER_RATE`) → 공격자 금고**, 나머지 20%·저장 식량 소멸
- 방어 유닛(귀속·배치) 전멸
- 영토 점유 이전 + 공격자에게 보호 기간 재시작
- 기존 건물은 그대로 인계(파괴된 성은 공격자가 수리)

---

## 7. 방어 설계 (사전 배치 중심 + 수비 어드밴티지)

방어자가 공성에 대비하는 수단(모두 사전·수동):

1. **방어 건물 배치**: 방벽(WALL def30)·타워(TOWER def60)를 지키려는 Zone에 건설 → 그 Zone DEF 상승 + 클리어 방패 역할.
2. **유닛 사전 배치**: 대기 유닛을 영토에 배치(deploy) → DEF 기여. 등급별 배치 슬롯 상한(성 레벨 + 숙소).
3. **성 HP·수리**: 성/방어 건물 HP를 수리해 다회 공성을 버팀. (수리 비용·속도 정책 TBD)
4. **외곽 방어 우선**: 외곽(Zone3) 방어를 두껍게 하면 공격자가 안쪽으로 진입 자체를 못 함.

### 수비 어드밴티지 (2026-07-13 확정)

방어 측에 구조적 우위를 준다:

- **① 정보 비대칭 (전장의 안개)**: 공격자는 **방어 유닛 구성을 볼 수 없다.** 공성 선언·미리보기 API는 방어 병력 종류/수량을 노출하지 않고, 타 유저의 영토 상세도 방어 배치를 숨긴다. 공격자는 결과 판정 후에야 규모를 체감한다.
- **② 수적 우위**: **한 번의 공격에 투입 가능한 공격 병력은 상한**(`SIEGE_ATTACKER_UNIT_CAP`, 값 TBD)이 있는 반면, 방어 수비대는 **위치 슬롯(성 레벨+숙소)만큼 대규모** 배치 가능 → 통상 수비 인원 > 공격 인원. 공격자는 다회 공성으로 소모전을 감수해야 한다.

> 능동 대응(카운트다운 중 증원)은 이번 범위에서 제외. 추후 확장 여지로만 기록.

---

## 8. 유닛 (스탯 역할 기반 — 명시적 상성 배수 없음)

**설계 원칙(2026-07-13 확정):** 가위바위보식 타입 배수는 두지 않는다. 대신 **스탯 3축**으로 역할을 가른다:
- `attackPower` — 교전(ATK>DEF) 기여
- `defensePower` — 방어 기여
- **`buildingDamage`(신규 스탯)** — 성공한 공성에서 **건물 HP를 깎는 양**. 교전 승리와 성벽 돌파를 분리한다.

즉 **교전은 전투 유닛(ATK/DEF)이 이기고, 성벽·성은 공성 병기(buildingDamage)가 부순다.** 순수 전투 유닛만으론 교전은 이겨도 건물을 빨리 못 깨고, 공성 병기만 데려오면 교전에서 전멸한다 → 조합 강제.

### 로스터 (기존 3종 + 신규 3종, 스탯은 시작값·튜닝 대상)

| 유닛 | 공격 | 방어 | 건물피해 | 식량 | 병영Lv | 역할 |
|---|---|---|---|---|---|---|
| INFANTRY 보병 | 10 | 10 | 낮음 | 1 | 1 | 균형·저비용 |
| ARCHER 궁수 | 8 | 18 | 낮음 | 2 | 1 | 방어 특화 |
| KNIGHT 기사 | 30 | 12 | 보통 | 4 | 2 | 공격(교전) 특화 |
| **CAVALRY 기병** (신규) | 22 | 10 | 낮음 | 3 | 2 | 기동·기습 — 위치 간 이동 비용/시간↓, 선제 |
| **SIEGE_ENGINE 공성 병기** (신규) | 5 | 5 | **매우 높음** | 6 | 3 | 대건물 특화. 유닛전 약함·느림 → 반드시 호위 필요 |
| **GUARD 수성 방어구** (신규) | 6 | 28 | - | 3 | 2 | 대공성 방어 — 매우 높은 DEF로 공성 병기 호위대를 교전에서 갈아냄 |

- **상호작용은 역할로 발생**(배수 없음): 공성 병기 ↔ 수성 방어구가 소프트 카운터. 공격자는 공성 병기+호위(기사/기병), 방어자는 수성 방어구+궁수+방벽으로 맞선다.
- **신규 유닛 추가 여지**: 필요 시 정찰(저비용 소모)·투석(원거리 건물)·중장보병 등으로 계속 확장 가능. (추가 여부 TBD)
- **정보 비대칭(§7-①)** 때문에 공격자는 방어 조합을 모른 채 공성 병기 비중을 걸어야 함 → 정찰/도박 요소.

---

## 9. 보호 / 점유 기간

- **보호 기간**: 영토 획득 직후 공성 불가 구간. (별도 `protectedUntil` 필드 도입 필요 — 현재 미분리)
- **점유 기간**: 보유 총 기간, 만료 시 재경매. 보호 < 점유.
- **값 TBD**: 점유 24h vs 3일, 보호 12h 등. 공성전과 함께 확정.
- **코드 작업**: `Territory.protectedUntil` 추가 → `occupy` 시 보호·점유 각각 설정 → `validateNotProtected`를 `protectedUntil` 기준으로.

---

## 10. 공격권 · 쿨다운 · 동시 공성

- **공격권 취득**: 아이템 상점 — 일반(AP100/GP500), 정밀(AP300).
- **재공격 쿨다운**: 공격 **실패** 후 같은 영토 `ATTACK_COOLDOWN_HOURS 2h` 재공격 불가.
- **동시 공성(TBD)**: 같은 영토에 여러 공격자/여러 PENDING 시즈 허용 여부, 판정 순서, Zone 클리어 상태 공유 여부 — 규칙 확정 필요.

---

## 11. 남은 결정 (TBD 요약)

1. `buildingDamage` 실제 수치(유닛별) + 성/건물 최대 HP 밸런스.
2. `SIEGE_ATTACKER_UNIT_CAP`(공격 1회 병력 상한) 값.
3. 성·건물 **수리** 비용·속도 정책.
4. 신규 유닛 추가 확장(정찰·투석 등) 여부.
5. 보호/점유 기간 값.
6. 동시 공성·다수 공격자 규칙.
7. Zone 클리어 상태의 지속/재생(방어자가 방어 건물 재건 시 클리어 해제되는가).

---

## 12. 코드 변경 요약(설계 확정 후)

| 대상 | 변경 |
|---|---|
| `MilitaryService.validateZoneCleared` | 진행 방향 반전(안쪽→바깥 전제) |
| `UnitType` + `unit-types.yml` | **`buildingDamage` 스탯 추가** + 신규 유닛 3종(CAVALRY·SIEGE_ENGINE·GUARD) |
| `SiegeService` 데미지 로직 | 단판 → 교전(ATK/DEF) + 건물 피해(Σ buildingDamage) 분리, 성 HP 누적 |
| 공격 병력 상한 | 공성 선언 시 `SIEGE_ATTACKER_UNIT_CAP` 검증 |
| 정보 비대칭 | 방어 유닛 구성 노출 API 차단(공성 미리보기·타 유저 영토 상세) |
| `Territory` | `protectedUntil` 필드 + `occupy` 시그니처 |
| `MilitaryService.validateNotProtected` | `protectedUntil` 기준 |
| 마이그레이션 | `unit_types.building_damage`, `territories.protected_until` 등 |
